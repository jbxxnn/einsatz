/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove the old i18n config
  // i18n: {
  //   locales: ['en', 'nl'],
  //   defaultLocale: 'en',
  // },
}

module.exports = nextConfig 